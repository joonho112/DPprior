% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_weights_rho.R
\name{compare_rho_w1}
\alias{compare_rho_w1}
\title{Compare rho and w1 Distributions}
\usage{
compare_rho_w1(a, b, M = .QUAD_NODES_DEFAULT)
}
\arguments{
\item{a}{Numeric; shape parameter of the Gamma prior on alpha (a > 0).}

\item{b}{Numeric; rate parameter of the Gamma prior on alpha (b > 0).}

\item{M}{Integer; number of quadrature nodes. Default is 80.}
}
\value{
A list containing:
\describe{
\item{mean_rho}{E(rho)}
\item{mean_w1}{E(w1)}
\item{mean_equal}{Logical; whether means are equal}
\item{var_rho}{Var(rho)}
\item{var_w1}{Var(w1)}
\item{var_ratio}{Var(rho) / Var(w1)}
}
}
\description{
Compares the marginal distributions of rho and w1 under the same
hyperprior alpha ~ Gamma(a, b).
}
\details{
While E(rho) = E(w1), the variances differ because:
\itemize{
\item \code{Var(rho | alpha) = 2*alpha / ((1+alpha)^2*(2+alpha)*(3+alpha))}
\item \code{Var(w1 | alpha) = alpha / ((1+alpha)^2*(2+alpha))}
}

Generally, Var(rho) < Var(w1) because rho averages over all squared weights.
}
\examples{
\dontrun{
compare_rho_w1(a = 2, b = 1)
compare_rho_w1(a = 1.6, b = 1.22)

}
}
\keyword{internal}
