% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13_error_bounds.R
\name{poisson_kl_divergence}
\alias{poisson_kl_divergence}
\title{Poisson-Poisson KL Divergence}
\usage{
poisson_kl_divergence(lambda, lambda_prime)
}
\arguments{
\item{lambda}{Numeric; mean of first Poisson distribution.}

\item{lambda_prime}{Numeric; mean of second Poisson distribution.}
}
\value{
Numeric; KL divergence (non-negative, possibly Inf).
}
\description{
Computes the Kullback-Leibler divergence between two Poisson distributions:
\deqn{KL(\text{Poisson}(\lambda) || \text{Poisson}(\lambda')) =
      \lambda \log(\lambda/\lambda') + \lambda' - \lambda}
}
\details{
Special cases:
\itemize{
\item If both \eqn{\lambda = 0} and \eqn{\lambda' = 0}: KL = 0
\item If \eqn{\lambda = 0} and \eqn{\lambda' > 0}: KL = \eqn{\lambda'}
\item If \eqn{\lambda > 0} and \eqn{\lambda' = 0}: KL = Inf
}
}
\keyword{internal}
